@model GoogleRegisterDTO

<div class="rwt-split-area rn-section-gap">
    <div class="wrapper">
        <div class="rn-splite-style">
            <div class="split-wrapper">
                <div class="row g-0 radius-10 align-items-center">
                    <div class="col-lg">
                        <div class="thumbnail">
                            <div class="holder">
                                <div class="bar"></div>
                                <iframe class="frame" src='https://my.spline.design/noisedisplacecopy-11481c1cb7e92b46bddb6e8c6d0a0c04/' frameborder='0' style="width: 800px; height:400px;"></iframe>
                            </div>
                        </div>
                    </div>
                    <div class="col-lg-4" style="margin-right:30px;">
                        <form action=@Url.Action("GoogleSignUp", "Auth") method="post" class="contact-form-1 rwt-dynamic-form">
                            <div class="form-group">
                                <input asp-for="PhoneNumber" placeholder="Your phone number">
                                <span asp-validation-for="PhoneNumber" class="form-error"></span>

                            </div>

                            <div class="form-group">
                                <input asp-for="Country" placeholder="Country">
                                <span asp-validation-for="Country" class="form-error"></span>
                            </div>

                            <div class="form-group">
                                <input asp-for="City" placeholder="City">
                                <span asp-validation-for="City" class="form-error"></span>
                            </div>

                            <div class="form-group">
                                <input asp-for="Address" placeholder="Address">
                                <span asp-validation-for="Address" class="form-error"></span>
                            </div>

                            <div class="form-group">
                                <button type="submit" class="btn-default btn-large rn-btn"
                                        style="background-color: #FF3CAC; cursor: pointer;">
                                    <span>Complete Sign up</span>
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
@if (ViewData.ContainsKey("MessageSent") && (bool)ViewData["MessageSent"] == true)
{
    <script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>

        function OpenWindow() {
            window.open("https://localhost:5001/", "_self")
        }

        function JSalert() {
            Swal.fire({
                position: 'center',
                icon: 'success',
                title: '<h4 class="modal-alert"> A confirmation email has been sent to you </h4>',
                showConfirmButton: false,
                confirmButtonColor: '#3085d6'
                //timer: 5000
            }).then((result) => {
                OpenWindow();
            })

        }

        JSalert();

    </script>
}


@*@section Scripts{
        <partial name="_ValidationScriptsPartial" />
    }*@
